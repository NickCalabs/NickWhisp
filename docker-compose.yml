services:
  whisp:
    build: .
    ports:
      - "${PORT:-8787}:8787"
    environment:
      - API_KEY=${API_KEY}
      - WHISPER_SERVER_URL=${WHISPER_SERVER_URL}
    tmpfs:
      - /tmp/whisp:size=2G
    restart: unless-stopped
